<%= form_with(model: article, html: { id: "article-form" }) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :content, id: 'tiptap-hidden' %>
  <div id="tiptap-editor" data-content="<%= article.content %>"></div>
  <div class="toc-container">
    <div id="toc" class="top-12"></div>
  </div>
<% end %>
