<div id="app"></div>
<h1>https://bucket.noteos.org/public/</h1>
<%= asset_path '/.vite/manifest-assets.json' %>
<div class="p"><%= link_to 'Create New Article', new_article_path if user_signed_in? %></div>
<div class="grid grid-cols-2 md:grid-cols-4 lg:gird-cols-5 xl:grid-cols-6 gap-2">
  <% @articles.each do |article| %>
    <div class="border rounded border-gray-400 hover:bg-gray-200">
      <%= link_to article.title, edit_article_path(article), class: "text-base px-3 py-2 block h-full" %>
      <!--    <p><%#= truncate(strip_tags(article.content.to_s), length: 100) %></p>-->
    </div>
  <% end %>
</div>
